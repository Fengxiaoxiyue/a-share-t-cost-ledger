# A 股做 T 成本簿 v1.2.0

这一版新增“截图 OCR 导入交易”，让券商成交记录从截图进入待校对区，再安全写入现有交易统计。

## 亮点

- 支持把券商成交表格截图拖入网页，由本机 Umi-OCR 离线识别。
- 利用 OCR 的文字坐标恢复表格行列，当前支持横向成交表格和纵向详情页。
- 同时导入买入、卖出、现金分红和股息红利税记录，并按现有现金流水规则校验。
- 识别结果先进入可编辑校对表，不会自动写入正式数据。
- 自动检查必填字段、整手数量、历史负持仓、价格×数量与成交金额、股票代码/名称以及疑似重复交易。
- 支持多张截图按顺序识别，避免本地 OCR 服务并发过载。
- 新增一键启动入口，可自动检测/启动 Umi-OCR，并确认服务只监听 localhost。

## 首次使用

1. 运行 `scripts\install-umi-ocr.ps1`，脚本只从 Umi-OCR 官方 GitHub Release 下载最新稳定 Rapid 版，并校验 SHA-256。
2. 双击 `start-local.cmd`。
3. 在网页选择“截图 OCR 导入”，看到“Umi-OCR ● 已连接”后拖入截图。
4. 校对每一行并选择归属股票，最后点击“确认导入”。

## 隐私与安全

交易截图只发送到 `127.0.0.1` 或 `localhost`，不会调用云 OCR、第三方分析或遥测服务。网页会拒绝配置为其他主机的 OCR 地址；启动脚本也会检查实际监听地址。

## 当前限制

- 当前支持常见的现金分红和股息红利税字段；其他公司行动或特殊券商字段仍会提示人工处理。
- 需要先在工具中添加对应股票，截图代码/名称不一致时会提示人工确认。
- 不同券商的列名和版式可能需要继续补充别名；低置信度或数学不一致的字段必须人工修正。
